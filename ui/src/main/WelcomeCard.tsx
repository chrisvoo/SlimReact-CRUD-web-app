import React from 'react';

export const WelcomeCard = () => (
  <div className="card">
    <h5 className="card-header">Welcome!</h5>
    <div className="card-body">
      <p>Select <i>Explore</i> items to list the departments and the employees.</p>
      <p>Select <i>Reports</i> items to list the predefined reports (MySQL views).</p>
    </div>
  </div>
);
